#include "main.h"
/**
 * mul - multiply two integers
 * @a: first integer
 * @b: second integer
 * Return: result (the multiplication of a and b)
 */
int mul(int a, int b)
{
	int result = (a * b);
		return (result);
}
